# coding: utf-8

"""
    RescueGroups.org API

    The RescueGroups.org REST API v5 provides access to adoptable pet data including animals, organizations, breeds, species, colors, and patterns. It supports advanced search with geodistance filtering, pagination, and relationship inclusion. API key authorization is used for public data access; bearer token authorization is used for private/write operations.

    The version of the OpenAPI document: 5.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class AnimalAttributes(BaseModel):
    """
    AnimalAttributes
    """ # noqa: E501
    name: Optional[StrictStr] = Field(default=None, description="Animal name.")
    birth_date: Optional[date] = Field(default=None, description="Animal birth date.", alias="birthDate")
    sex: Optional[StrictStr] = Field(default=None, description="Animal sex.")
    age_group: Optional[StrictStr] = Field(default=None, description="Age group category.", alias="ageGroup")
    size_group: Optional[StrictStr] = Field(default=None, description="Size group category.", alias="sizeGroup")
    is_adoption_pending: Optional[StrictBool] = Field(default=None, description="Whether adoption is pending.", alias="isAdoptionPending")
    is_altered: Optional[StrictBool] = Field(default=None, description="Whether the animal is spayed/neutered.", alias="isAltered")
    picture_count: Optional[StrictInt] = Field(default=None, description="Number of pictures available.", alias="pictureCount")
    video_count: Optional[StrictInt] = Field(default=None, description="Number of videos available.", alias="videoCount")
    adopted_date: Optional[date] = Field(default=None, description="Date the animal was adopted.", alias="adoptedDate")
    special_needs_details: Optional[StrictStr] = Field(default=None, description="Description of any special needs.", alias="specialNeedsDetails")
    description_text: Optional[StrictStr] = Field(default=None, description="Plain text description of the animal.", alias="descriptionText")
    location_citystate: Optional[StrictStr] = Field(default=None, description="City and state where the animal is located.", alias="locationCitystate")
    location_state: Optional[StrictStr] = Field(default=None, description="State where the animal is located.", alias="locationState")
    location_distance: Optional[Union[StrictFloat, StrictInt]] = Field(default=None, description="Distance from search location.", alias="locationDistance")
    rescue_id: Optional[StrictStr] = Field(default=None, description="External rescue ID.", alias="rescueId")
    url: Optional[StrictStr] = Field(default=None, description="URL of the animal profile page.")
    __properties: ClassVar[List[str]] = ["name", "birthDate", "sex", "ageGroup", "sizeGroup", "isAdoptionPending", "isAltered", "pictureCount", "videoCount", "adoptedDate", "specialNeedsDetails", "descriptionText", "locationCitystate", "locationState", "locationDistance", "rescueId", "url"]

    @field_validator('sex')
    def sex_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Male', 'Female', 'Unknown']):
            raise ValueError("must be one of enum values ('Male', 'Female', 'Unknown')")
        return value

    @field_validator('age_group')
    def age_group_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Baby', 'Young', 'Adult', 'Senior']):
            raise ValueError("must be one of enum values ('Baby', 'Young', 'Adult', 'Senior')")
        return value

    @field_validator('size_group')
    def size_group_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['Small', 'Medium', 'Large', 'Extra Large']):
            raise ValueError("must be one of enum values ('Small', 'Medium', 'Large', 'Extra Large')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of AnimalAttributes from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of AnimalAttributes from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "name": obj.get("name"),
            "birthDate": obj.get("birthDate"),
            "sex": obj.get("sex"),
            "ageGroup": obj.get("ageGroup"),
            "sizeGroup": obj.get("sizeGroup"),
            "isAdoptionPending": obj.get("isAdoptionPending"),
            "isAltered": obj.get("isAltered"),
            "pictureCount": obj.get("pictureCount"),
            "videoCount": obj.get("videoCount"),
            "adoptedDate": obj.get("adoptedDate"),
            "specialNeedsDetails": obj.get("specialNeedsDetails"),
            "descriptionText": obj.get("descriptionText"),
            "locationCitystate": obj.get("locationCitystate"),
            "locationState": obj.get("locationState"),
            "locationDistance": obj.get("locationDistance"),
            "rescueId": obj.get("rescueId"),
            "url": obj.get("url")
        })
        return _obj


