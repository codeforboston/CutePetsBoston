# Ruby
template.env
*.gem
*.rbc
.DS_Store
.bundle
.config
coverage
InstalledFiles
lib/bundler/man
pkg
rdoc
spec/reports
test/tmp
test/version_tmp
tmp

# Python
.venv
__pycache__

# YARD artifacts
.yardoc
_yardoc
doc/
image.png

# Other - .env is unused but added here just in case
.env
.env.local

# Ignore because this is used by github artifacts
database.json

# The redirect slug->URL mapping only lives on the gh-pages branch (see
# .github/workflows/prod.yml and pages-sync.yml); it should never be
# committed to master, even if a local run creates it here.
docs/redirects.json
